apiVersion: v1
kind: Service
metadata:
  name: {{ include "disaggregatedset.fullname" . }}-controller-manager-metrics-service
  labels:
    control-plane: controller-manager
  {{- include "disaggregatedset.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    app.kubernetes.io/name: disaggregatedset
    control-plane: controller-manager
    {{- include "disaggregatedset.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.metricsService.ports | toYaml | nindent 2 }}
