apiVersion: scheduling.k8s.io/v1alpha2
kind: PodGroup
metadata:
  name: lws-workload-pg
  namespace: default
spec:
  podGroupTemplateRef:
    workload:
      workloadName: lws-workload
      podGroupTemplateName: lws-pg
  schedulingPolicy:
    gang:
      minCount: 4